def sym(l):
    """Returns whether a list is symmetric or not
    >>> sym ([])
    True
    >>> sym ([1])
    True
    >>> sym ([1, 4, 5, 1])
    False
    >>> sym ([1, 4, 4, 1])
    True
    >>> sym (['l', 'o', 'l'])
    True
    """

